// Objeto Math

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5); /* redondear*/
resultado = Math.ceil(2.1); // redondea hacia arriba
resultado = Math.floor(2.9);  //redondea hacia abajo
resultado = Math.sqrt(144); // raiz cuadrada
resultado = Math.abs(-200);  // convierte el nuemro en +
resultado = Math.min( 1 , 2, 3, 7, 8, 10 ); // te muestra el numero menor
resultado = Math.max( 6, 7, 8, 2, 1 ); // te muestra el numero mayor
resultado = Math.random();
resultado = Math.floor( Math.random() * 30);

console.log(resultado);
